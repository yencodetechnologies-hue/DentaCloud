# Frontend doesn't need backend secrets or URLs.
# It always calls the API via same-origin `"/api"` and relies on:
# - Vite dev proxy (see `vite.config.js`)
# - Vercel rewrites (see `vercel.json` / build script)
