# Local dev only — Vite proxies /api to localhost:1478
# VITE_API_URL=

# Vercel build only — backend origin (no /api suffix). Generates vercel.json proxy.
# Example: https://evident-dental-api.onrender.com
# API_URL=
